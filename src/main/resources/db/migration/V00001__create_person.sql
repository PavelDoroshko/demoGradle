CREATE  TABLE  IF NOT EXISTS person (
                                     id bigserial NOT NULL PRIMARY KEY ,
                                     name varchar (20),
                                     age int
    ) ;